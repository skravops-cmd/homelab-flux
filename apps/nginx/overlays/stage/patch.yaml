# Stage patch: override base replicas to 3.
# Stage should mimic production with multiple replicas for availability.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
  namespace: nginx
spec:
  replicas: 3
