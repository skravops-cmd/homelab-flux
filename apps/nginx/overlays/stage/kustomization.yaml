# Stage overlay for nginx.
# Inherits the base resources and applies environment-specific patches.
# The stage cluster (k3s) is production-like, so we run multiple replicas.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  - path: patch.yaml
