# Kustomize base for nginx.
# Bundles the Namespace, Deployment, and Service into a single unit
# that overlays can reference and patch.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
