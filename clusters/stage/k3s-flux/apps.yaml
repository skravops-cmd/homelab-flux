# Application Kustomization for the k3s (stage) cluster.
# Separate from infrastructure.yaml so apps can be toggled independently.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../../apps/nginx/overlays/stage
  - ../../../apps/uptime-kuma/overlays/stage
