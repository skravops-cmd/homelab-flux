# Kustomize base for Traefik ingress controller.
# Traefik installs into kube-system (matching stock k3s behavior),
# so no separate Namespace resource is needed.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - helmrelease.yaml
