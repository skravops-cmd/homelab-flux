# Kustomization that bundles all HelmRepository sources into a single unit.
# Included by each cluster's infrastructure.yaml so every cluster gets
# the same set of available chart repositories.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - bitnami.yaml
  - helmforge.yaml
  - traefik.yaml
