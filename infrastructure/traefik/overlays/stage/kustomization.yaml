# Stage overlay for Traefik (k3s cluster).
# Pure inherit from base — no patches needed.
# k3s's built-in ServiceLB assigns a real LoadBalancer IP automatically.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
