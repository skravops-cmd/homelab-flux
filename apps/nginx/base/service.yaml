# Cluster-internal Service exposing nginx on port 80.
# Routes traffic to any pod with the label "app: nginx".
apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: nginx
spec:
  selector:
    app: nginx
  ports:
    - port: 80
      targetPort: 80
